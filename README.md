### Dev

- Run vite server `npm run dev`
- No errors should appears regarding peer-deps as only `<Button>` is used, and peer-deps `@radix-ui/slot` is installed ✔️

### Build
- Run vite server `npm run build`
- Same
